# myServerInitializeScript

Install gcc, g++, gdb, python, python3, python-pip, python3-pip, vim, zsh, etc...å

Install oh-my-zsh

Install tmux

Configure the vim

Add some choices for install some other software such as php, apache, nginx, etc...
